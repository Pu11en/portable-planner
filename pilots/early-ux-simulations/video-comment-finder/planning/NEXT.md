# Continue This Plan

Plan location: `/home/drewp/main-projects/portable-planner/pilots/early-ux-simulations/video-comment-finder/planning/`

Load `PLAN.md` first. Follow its lifecycle status:

- `planning`: load `decisions/P-001-define-useful-result.md` and only its linked evidence or decisions.
- `awaiting approval`: show `PLAN-VIEW.md` and obtain explicit approval; do not build.
- `approved for build`: load the first eligible execution ticket named by `PLAN.md` and use the harness's normal build workflow; Portable Planner does not perform the work.

Treat those files as canonical; do not rely on previous chat.

Work only on **P-001 — Define the Useful Result**.

Essential context: Drew supplies the trusted channels. The future skill finds videos and comments only within them, returns a small set of directly linked and explained opportunities Drew is qualified to answer, then stops. This live pilot plans only; it must not implement or operate the finder. The current human-owned choice is which qualifying comments should rise first. Before presenting the abstract `A/B/C` categories, show one literal example comment Drew can picture.

This session must: settle the useful-result contract and produce a cohesive, visual, execution-ready plan without pre-creating speculative planning tickets.

Complete when: the current preference and remaining behavior are settled or derived, the finish audit passes, dependency-ordered one-session execution tickets cover the complete route, and the visual plan is ready for Drew's explicit approval.
