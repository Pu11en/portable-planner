# Continue This Plan

Plan location: `planning/`

Load `PLAN.md` first, then `decisions/P-001-define-finder-contract.md` and only its linked evidence or decisions. Treat those files as canonical; do not rely on previous chat.

Work only on **P-001 — Define the Finder Contract**.

Essential context: The skill must present several worthwhile, directly linked YouTube comment questions that Drew is qualified to answer, then stop before answer creation. Drew chooses, opens, and answers one manually. The source mode and filtering/output contract remain unresolved; begin with source mode.

This session must: Confirm the source mode, worthwhile-question rule, qualification evidence, output fields, and hard-stop behavior, then reconcile the plan and produce dependency-ordered execution tickets if the finish audit passes.

Complete when: The source mode, worthwhile-question rule, qualification evidence, output fields, and hard-stop behavior are explicit; their tradeoffs and downstream effects agree with `PLAN.md`; and the finish audit can produce dependency-ordered, session-sized execution tickets without guessing.
