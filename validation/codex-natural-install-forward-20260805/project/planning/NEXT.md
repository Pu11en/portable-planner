# Continue This Plan

Plan location: `/home/drewp/main-projects/portable-planner/validation/codex-natural-install-forward-20260805/project/planning/`

Load `PLAN.md` first. Follow its lifecycle status:

- `planning`: load `decisions/P-001-define-workshop.md` and only its linked evidence or decisions.
- `awaiting approval`: show `PLAN-VIEW.md` and obtain explicit approval; do not run the workshop.
- `approved for build`: load the first eligible file in `execution/` and use the harness's normal workflow; Portable Planner does not perform the work.

Treat those files as canonical; do not rely on previous chat.

Work only on **P-001 — Define the workshop and complete the plan**.

Essential context: The workshop is free, on Saturday, lasts two hours, uses the library's existing room and Wi-Fi, serves adults with mixed devices and confidence levels, and promises safe independent completion of one everyday digital task. Planning only; do not run it.

Confirmed decision: use a capped coaching workshop with advance registration and task screening. The accepted tradeoff is lower attendance in exchange for a credible independent-completion promise.

Confirmed task-scope decision: advertise a short supported menu and allow screened custom requests. Accept a custom request only when staff can prepare for it, coach it safely on the participant's own device, and preserve time for an independent repeat. Redirect unsafe, overly complex, or staff-performed tasks before the event.

This session must: choose the concrete task families to name on the first supported menu, then use that menu and the custom-request rule to derive capacity and helper coverage.

Complete when: the supported menu is explicit, its effect on capacity and helper coverage is recorded consistently in the current decision and `PLAN.md`, and the next action is exact.
